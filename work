#!/usr/bin/env node

require('lesswork-framework/lib/env');

const app = require('./config/app');

require('./bootstrap/app')({}, function () {
  require('lesswork-framework/src/Work')(app);
}, app.providers.concat(app.workProviders));