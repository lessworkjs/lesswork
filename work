#!/usr/bin/env node

require('lesswork-framework/lib/env');

const Kernel = require('lesswork-framework/src/Route/Kernel');

const config = require('./config/app');

Kernel({}, function () {
  require('lesswork-framework/src/Work')(config);
}, config.providers.concat(config.workProviders));